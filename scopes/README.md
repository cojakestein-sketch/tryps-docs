# Tryps — Scopes of Work

Each phase contains scopes. Each scope gets its own spec, FRD, and plan — generated via `/vision`.

## Structure

```
scopes/
├── p1/                          # Phase 1: Core App
│   ├── core-flows/              # 1. 19 user flows from flow tracker
│   ├── tooltips-teaching/       # 2. Tooltips & teaching the user
│   ├── notifications-voting/    # 3. Notifications & voting (48hr rules)
│   ├── post-trip-review/        # 4. Post-trip review & montage
│   ├── travel-dna/              # 5. Travel DNA algorithm & NL
│   └── recommendations/         # 6. Recommendation algorithm (5 sub-scopes)
├── p2/                          # Phase 2: Stripe + Linq (future)
└── p3/                          # Phase 3: Agent Layer (future)
```

## Per-Scope Files

Each scope folder contains:

| File | Purpose | Generated by |
|------|---------|-------------|
| `spec.md` | Acceptance criteria & intent | `/vision /spec` |
| `frd.md` | Functional requirements | `/vision /frd` |
| `plan.md` | Implementation plan | `/vision /plan` |
| `status.md` | Current status & notes | Manual / Marty |

## Workflow

1. Jake runs `/vision` on a scope → generates spec → FRD → plan
2. Spec becomes a ClickUp ticket for the dev team
3. FRD renders inline in Mission Control Gantt (accordion detail)
4. Plan guides implementation
5. Status updated by Marty or manually

## Access

- **Jake**: Full read/write (this repo + private planning in `t4/_private/`)
- **Asif & Nadeem**: Read/write (GitHub collaborators)
- **Marty**: Read (via GitHub API, renders FRDs in Mission Control)
- **Krisna & Arslan**: Read (public repo, view via GitHub)
