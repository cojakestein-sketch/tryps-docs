---
title: "Checkout — March 25, 2026"
date: 2026-03-25
type: checkout
---

# Checkout — March 25, 2026

---

## Asif — iMessage Agent + Onboarding Deep Link

### What We Built: Daily Trip Card for iMessage Groups

**Problem:** Send a visual trip summary in iMessage groups every morning so users can see trip status at a glance.

**Solution:** Three-part system:

#### 1. OG Image Generator (`supabase/functions/trip-card-image/`)

- Generates a 1200x630 PNG dynamically using Satori + resvg-wasm
- Shows: cover photo, Tryps logo, status badge ("11 days away" / "Happening Now"), trip name, dates, location, avatar stack, activity thumbnails, footer
- URL: `/functions/v1/trip-card-image?id={tripId}`

#### 2. Trip Card Web Page (`website-tryps/app/trip/[id]/page.tsx`)

- Next.js page deployed on Vercel (`website-tryps-sage.vercel.app`)
- Card 2 (detailed overview): trip name, flights, accommodation, vibes, expenses, packing progress, day-by-day itinerary timeline
- OG meta tags point to the image generator for link previews
- Env vars added to Vercel: `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`

#### 3. Daily Facilitator Update (`close-polls/index.ts` + `daily-facilitator.ts`)

- Updated to send 2 messages per trip per day:
  - **Message 1:** Trip card image (sent as Linq media attachment — shows inline, no "Click to Load Preview") + trip URL
  - **Message 2:** Text nudge based on completeness level

### Outstanding Items

- **Universal Links** — tapping the link opens the web page, not the app yet. Needs `jointryps.com` domain connected to the Vercel project (Jake owns this) or `associatedDomains` updated for the Vercel URL
- **Design refinement** — Card design works but could be polished further to match Figma pixel-perfectly
- **jointryps.com domain** — Currently using `website-tryps-sage.vercel.app`. Once Jake connects the domain, update `TRIP_CARD_BASE_URL` env var

---

## Nadeem — Overview Tab Redesign + SC Audit

- Built all 7 overview section cards from scratch (Flights, Stay, Activities, Vibe, Packing, Expenses, Itinerary) — 1,250+ lines, 16 files changed on `feat/overview-tab`
- Each card has filled state with StatusBadge (confirmed/unconfirmed) + empty state with pill-shaped "Add X" CTAs
- Audited all 48 SC from `output-backed-screen/spec.md`: **7 implemented, 30 not implemented, 11 blocked** (iMessage infographic scope)
- Updated SC tracking table in March 25 standup
- Major remaining work: completeness progress bar (SC-3), drag-and-drop itinerary (SC-9–13), celebration animation (SC-31–33), past trip freeze (SC-34–35)

---

## Rizwan — Agent Intelligence Production Integration

### What We Built: Memory System Deployed to Production

**Problem:** The AI memory system (signal extraction, preference profiles, recommendations, vote-on-behalf) was working in agent-demo but not integrated into the main Tryps app.

**Solution:** Full production integration across multiple Edge Functions and database.

#### 1. Database Migrations (3 new migrations pushed to production Supabase)

- `20260325000000_agent_intelligence_tables.sql` — 9 tables: `user_memory`, `trip_memory`, `vote_inferences`, `signal_extraction_log`, `destination_patterns`, `activity_templates`, `recommended_activities`, `recommendation_events`, `proactive_message_log`
- `20260325010000_agent_intelligence_triggers.sql` — 5 implicit learning triggers: vote override correction signals, activity adoption signals, post-trip favorite signals, explicit vote guard, vibe-to-memory sync
- `20260325020000_agent_intelligence_cron.sql` — nightly destination pattern rebuild at 3 AM UTC
- 3 RPCs: `upsert_user_memory_signal()`, `get_user_preference_profile()`, `check_proactive_cooldown()`

#### 2. Edge Functions Deployed (8 functions)

- **trip-chat** — now calls `buildUserContext()` to personalize responses + `fireAndForgetExtraction()` to silently learn preferences from every message
- **suggest-packing** — now injects user preference profile into packing prompt (adventure traveler → hiking gear, beach lover → snorkel gear)
- **explore-chat-v2** — already had extraction wired (confirmed working)
- **extract-memory-signals** — GPT-4o-mini signal extraction pipeline (new deploy)
- **recommend-activities** — hybrid recommendation engine: content 40% + collaborative 30% + social 30% (new deploy)
- **vote-on-behalf** — vote inference from preference profiles (new deploy)
- **sync-dna-to-memory** + **sync-vibe-to-memory** — sync Travel DNA and Vibe Quiz answers to memory (new deploy)

#### 3. PR #303 raised against develop

- Branch: `feat/agent-intelligence-memory`
- 44 files changed, 6,500+ lines
- All pre-commit hooks passed (typecheck, eslint, prettier)

#### 4. OVERVIEW.md created at repo root

- System-wide onboarding doc explaining all 3 repos, their stacks, AI integrations, and how they connect

### Outstanding Items

- **iMessage agent memory integration** — attempted wiring `buildUserContext` + `fireAndForgetExtraction` into `ai/agent-router.ts` (the OpenAI Responses API path that linq-worker uses). Crashed the worker on deploy — reverted to restore messaging. Needs proper try/catch wrapping before re-deploying.
- **Linq webhook delivery paused** — Linq stopped sending webhooks after the failed deploy. Need to check Linq dashboard for delivery status or re-register webhook URL. **Blocker for Asif.**
- **SC-32 (cross-trip accuracy)** — multi-trip eval harness not built yet, deferred to tomorrow
- **Stale PRs #299 and #297** — need to close
